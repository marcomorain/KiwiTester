machine:
  environment:
    LANG: en_US.UTF-8
dependencies:
  pre:
    - 'echo "gem: --no-ri --no-rdoc" >> ~/.gemrc'
    - sudo gem install cocoapods --pre
    - pod setup
  cache_directories:
    - Pods
